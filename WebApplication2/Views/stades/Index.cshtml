@model IEnumerable<WebApplication2.Models.stade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.stade_nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ville.ville_lib)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.stade_nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ville.ville_lib)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.stade_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.stade_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.stade_id })
        </td>
    </tr>
}

</table>
